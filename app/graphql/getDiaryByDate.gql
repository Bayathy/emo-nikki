query GetDiaryByDate($uid: String!, $date: date!) {
  users(order_by: {date: asc}, where: {uid: {_eq: $uid}, sentiments: {date: {_eq: $date}}}) {
		sentiments {
      negative
      positive
    }
    diaries {
      text
    }
    questions {
      question
      answer
    }
  }
}
